# v1.3.25

- Expand Admin → Appearance → PWA App with SuperPWA / vite-plugin-pwa fields: 192×192 and 512×512 icons, screenshots, categories, `display_override` (window-controls-overlay), `handle_links`, `launch_handler`, and optional share target.
- Add opt-in service worker (`/pwa-sw.js`) plus `/offline.html`. Both stay **off** until enabled, so production cache is not hijacked.
- Native chrome in the installed app gains pull-to-refresh, an offline banner and an update prompt. Regular browser tabs on necrotixlab.com stay unchanged.
- Install banners remain off by default. No Prisma schema change. Protected design files are untouched.
