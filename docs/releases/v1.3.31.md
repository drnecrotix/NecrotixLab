# v1.3.31

- Generate the whole PWA icon pack from one source logo. 192, 512, Apple 180, maskable and monochrome no longer need separate uploads; per-size overrides stay behind an advanced toggle.
- Read CMS media (`/uploads`, `/media`) when building those PNGs, so a library JPG becomes a proper icon pack.
- Add launch splash layouts: logo, logo + name, wordmark, custom image, or color only, with tagline and duration.
- Add journal reader mode in the installed app: paper / sepia / night / system, type size, hides related chrome. Regular browser tabs are unchanged.
