# v1.3.27

- Ship real PNG app icons (96 / 180 / 192 / 512), a maskable Android icon and a monochrome themed icon, plus per-device Apple startup images generated from the PWA background color.
- Add phone and desktop install screenshots to the web app manifest so Chrome's install UI is no longer empty.
- Give home-screen shortcuts their own 96×96 icons.
- When the opt-in service worker is enabled, cache Journal, Projects, Gallery and Contact network-first so the installed app can be read offline.
- Show an app-icon badge for signed-in admins in standalone mode, counting new service requests and recent comments.
