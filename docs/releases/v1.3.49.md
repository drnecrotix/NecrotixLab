# v1.3.49 - G-Code workspace and interface fixes

- Adds an active, CMS-managed G-Code Editor with editable code, XY toolpath preview, line diagnostics, reusable command snippets and `.nc` download.
- Makes native dropdown menus readable in light and dark themes across the website.
- Shows the redesigned Back to top control only while the shared footer is visible.
- Adds parser unit coverage and the G-Code route to responsive visual smoke tests.
