# v1.3.57

- Video Download now installs and finds yt-dlp with the application's npm dependencies. N0C still requires Python 3.9+ and permission to run child processes.
- WHOIS uses IANA RDAP bootstrap registries for domains and IPv4/IPv6, then a secondary resolver if needed.
- Expanded WHOIS results with public registry contacts, network prefixes, DNSSEC, events, links and the registry source.
