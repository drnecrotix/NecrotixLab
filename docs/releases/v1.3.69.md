# v1.3.69

- Add optional Discord server member lookup when direct User ID lookup returns 404. A configured bot and server ID can provide a nickname, avatar and banner for members of that server.
- Add a limited Lanyard fallback for users enrolled in its service. Label the source and retain an unverified result when no provider has profile data.
