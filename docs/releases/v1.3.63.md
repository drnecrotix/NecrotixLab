# v1.3.63

- Send the token and user agent expected by X's public syndication endpoint when inspecting a post, and include media from quoted posts.
- Replace the blank third-party iframe with an external fallback link and a copy button. The post URL is not shared with X Downloader until the visitor pastes it there.
