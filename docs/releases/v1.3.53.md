# v1.3.53

- Animated CNC and Kreatrics Cloud SVG circles now have valid initial coordinates and radii.
- Browser console errors caused by undefined `cx`, `cy` and `r` attributes are prevented before animations start.
