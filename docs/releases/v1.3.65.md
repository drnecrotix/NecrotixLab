# v1.3.65

- Extend Discord Lookup with profile avatar, banner, public fields, invite splash, and server preview or widget artwork when Discord exposes them. Explain clearly when `DISCORD_BOT_TOKEN` is missing; arbitrary user profiles cannot be read anonymously.
- Accept canonical and `/t/` Threads post links, and list each available carousel clip and MP4 resolution.
- List higher-resolution YouTube MP4 video-only streams and an M4A audio stream alongside lower-resolution MP4 files that contain sound. HD video and audio require local merging because this server has no working FFmpeg executable.
