# v1.3.65

- Expand Discord Lookup with available public profile fields, including avatar, banner, avatar decoration asset, public flags, and primary server tag when a bot token is configured.
- Show invite splash images, additional invite details, and a prominent public server widget image.
- Add links to the available profile and server images. Discord's API still does not allow anonymous arbitrary user profile lookup.
