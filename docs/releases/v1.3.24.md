# v1.3.24

- Add a native PWA layer that only activates in installed display modes, so the public website in a regular browser stays unchanged.
- Add Admin → Appearance → PWA App controls for identity, tab bar, colors, icons, shortcuts and optional install prompts.
- Persist PWA configuration as a draft CMS page (`__pwa-config`) without a database schema migration.
- Drive `manifest.webmanifest` and Apple web-app metadata from those admin settings, keeping current NecrotixLab defaults until the first save.
