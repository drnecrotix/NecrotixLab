# v1.3.73

- Restore a compact Tools catalogue: square tiles, search, category tabs, result counts and a planned-tools toggle.
- Tighten the Addons admin into compact rows with a border tab strip.
- Show Manage when Tools is already current, Update Tools only when the installed package version is older than the CMS build, and a notice when GitHub is ahead of this build.
- Applying Update Tools writes the bundled package version, uses pending button labels, redirects to Installed and shows a success message.
- Keep default Tools entries for existing installations that have an empty configured list, and refresh the installable Tools ZIP.
